export async function refreshCommand(_args: string[]): Promise<void> {
  console.error('refresh is not implemented yet -- it arrives with the TCGdex collector in Phase 2.');
  process.exitCode = 1;
}
