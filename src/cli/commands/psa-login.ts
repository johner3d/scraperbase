export async function psaLoginCommand(_args: string[]): Promise<void> {
  console.error('psa-login is not implemented yet -- it arrives in Phase 3 (PSA browser foundation).');
  process.exitCode = 1;
}
